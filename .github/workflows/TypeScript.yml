name: 🏦 금융 자동화 통합 - 전체 echo/환경/보안/멀티모듈/빌드/스프링부트 최신화 (No Skip, No Blur, +DB echo)

on:
  workflow_dispatch:
  push:
    branches: [main]
  schedule:
    - cron: '0 2 * * *'

jobs:
  finance-ci-allinone:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v4

      - name: 📁 대량 echo/로그/백업/옵션/스프링부트 디렉토리 생성 및 echo파일
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          mkdir -p .github/echo_build || true
          ECHO_LIST=".github/echo_build/echo_dirlist_${TS}.txt"
          echo "[ECHO] 생성예정 디렉토리 목록 @ $TS" | tee $ECHO_LIST || true
          for d in \
            .github/echo_ts \
            .github/echo_js \
            .github/echo_java \
            .github/echo_upgrade \
            .github/echo_spring \
            .github/echo_env \
            .github/echo_secret \
            .github/echo_buildopt \
            .github/echo_multimodule \
            .github/logs_ts \
            .github/ts_projects \
            .github/ts_bin \
            .github/ts_backup \
            .github/ts_dist_backup \
            .github/js_projects \
            .github/js_backup \
            .github/js_dist_backup \
            .github/java_projects \
            .github/java_build \
            .github/java_multi \
            .github/mvn_install \
            .github/springboot_projects \
            .github/springboot_build \
            .github/springboot_backup \
            .github/springboot_logs \
            .github/springboot_multi \
            .github/build_history \
            .github/build_logs \
            .github/run_logs \
            .github/echo_compile \
            .github/echo_build \
            .github/echo_run \
            .github/echo_result \
            .github/migration; do
            ECHO_FILE=".github/echo_build/echo_mkdir_${TS}_$(basename $d).txt"
            echo "[ECHO][$TS] mkdir -p $d" > $ECHO_FILE || true
            echo "[ECHO][$TS] mkdir -p $d" >> $ECHO_LIST || true
            mkdir -p "$d" || echo "[FAIL][$TS] $d 생성 실패" >> $ECHO_FILE
            if [ -d "$d" ]; then
              echo "[SUCCESS][$TS] $d 생성됨" >> $ECHO_FILE || true
            else
              echo "[FAIL][$TS] $d 생성 실패" >> $ECHO_FILE || true
            fi
          done
          tree .github > .github/echo_build/echo_dirs_tree_${TS}.txt 2>/dev/null || ls -lR .github > .github/echo_build/echo_dirs_lslr_${TS}.txt
          echo "[ECHO][$TS] 전체 디렉토리 생성 및 에코파일 생성 완료" > .github/echo_build/echo_all_mkdir_result_${TS}.txt || true

      - name: 🔑 환경변수/보안/옵션/멀티모듈 에코파일 생성
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          echo "SPRING_PROFILES_ACTIVE=prod" > .github/echo_env/spring_profiles.env || true
          echo "JAVA_TOOL_OPTIONS=-Xms256m -Xmx1024m" >> .github/echo_env/spring_profiles.env || true
          echo "CI_BUILD_ID=$TS" >> .github/echo_env/spring_profiles.env || true
          echo "[ECHO] .github/echo_env/spring_profiles.env 생성 완료 ($TS)" > .github/echo_env/echo_spring_profiles_env_${TS}.txt || true
          echo "SECRET_DB_PW=****" > .github/echo_secret/secret.env || true
          echo "SPRING_API_KEY=****" >> .github/echo_secret/secret.env || true
          echo "[ECHO] .github/echo_secret/secret.env 생성 완료 ($TS)" > .github/echo_secret/echo_secret_env_${TS}.txt || true
          echo "TS_BUILD_OPTS=--strict --target ES2020" > .github/echo_buildopt/ts_buildopt.txt || true
          echo "JAVA_BUILD_OPTS=-DskipTests=true -Dmaven.test.failure.ignore=true" > .github/echo_buildopt/java_buildopt.txt || true
          echo "SPRING_BUILD_OPTS=--debug --no-daemon" > .github/echo_buildopt/spring_buildopt.txt || true
          echo "[ECHO] echo_buildopt 생성 완료 ($TS)" > .github/echo_buildopt/echo_buildopt_${TS}.txt || true
          mkdir -p .github/springboot_multi/app-api/src/main/java/com/fin/appapi || true
          mkdir -p .github/springboot_multi/app-core/src/main/java/com/fin/appcore || true
          mkdir -p .github/springboot_multi/app-web/src/main/java/com/fin/appweb || true
          echo "rootProject.name = 'finance-multi'" > .github/springboot_multi/settings.gradle || true
          echo "include 'app-api', 'app-core', 'app-web'" >> .github/springboot_multi/settings.gradle || true
          echo "[ECHO] .github/springboot_multi/settings.gradle 및 디렉토리 구조 생성 완료 ($TS)" > .github/echo_multimodule/echo_settings_gradle_${TS}.txt || true

      - name: 🗄️ 환경별 DB echo config/.env/.sql 파일 자동생성
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          echo "// DB CONFIG ECHO" > .github/echo_ts/db_config.ts || true
          echo "export const DB_CONFIG = { host: 'localhost', user: 'finance', password: 'echo_pw', database: 'financedb', port: 3306 };" >> .github/echo_ts/db_config.ts || true
          echo -e "DB_HOST=localhost\nDB_USER=finance\nDB_PASS=echo_pw\nDB_NAME=financedb" > .github/echo_ts/.env || true
          echo "CREATE TABLE IF NOT EXISTS ts_sample (id INT PRIMARY KEY, amount DECIMAL(12,2), memo VARCHAR(100));" > .github/echo_ts/table_info.sql || true
          echo "[ECHO] TypeScript 환경 DB 파일 생성" > .github/echo_ts/echo_db_config_${TS}.txt || true
          echo '{ "host": "localhost", "user": "finance", "password": "echo_pw", "database": "financedb", "port": 3306 }' > .github/echo_js/dbinfo.json || true
          echo -e "DB_HOST=localhost\nDB_USER=finance\nDB_PASS=echo_pw\nDB_NAME=financedb" > .github/echo_js/.env || true
          echo "CREATE TABLE IF NOT EXISTS js_logs (id INT PRIMARY KEY, msg VARCHAR(255), created_at DATETIME);" > .github/echo_js/table_info.sql || true
          echo "[ECHO] JS 환경 DB 파일 생성" > .github/echo_js/echo_db_config_${TS}.txt || true
          echo "spring.datasource.url=jdbc:mysql://localhost:3306/financedb" > .github/echo_java/application.properties || true
          echo "spring.datasource.username=finance" >> .github/echo_java/application.properties || true
          echo "spring.datasource.password=echo_pw" >> .github/echo_java/application.properties || true
          echo "CREATE TABLE IF NOT EXISTS java_audit (id BIGINT AUTO_INCREMENT PRIMARY KEY, event VARCHAR(128), ts TIMESTAMP);" > .github/echo_java/table_info.sql || true
          echo "[ECHO] Java 환경 DB 파일 생성" > .github/echo_java/echo_db_config_${TS}.txt || true
          echo -e "spring:\n  datasource:\n    url: jdbc:mysql://localhost:3306/financedb\n    username: finance\n    password: echo_pw\n    driver-class-name: com.mysql.cj.jdbc.Driver" > .github/echo_spring/application.yml || true
          echo "CREATE TABLE IF NOT EXISTS spring_tasks (task_id INT AUTO_INCREMENT PRIMARY KEY, desc VARCHAR(255), completed BOOLEAN);" > .github/echo_spring/table_info.sql || true
          echo -e "MYSQL_USER=finance\nMYSQL_PW=echo_pw" > .github/echo_spring/.env || true
          echo "[ECHO] SpringBoot 환경 DB 파일 생성" > .github/echo_spring/echo_db_config_${TS}.txt || true
          echo "CREATE TABLE IF NOT EXISTS customers (id INT PRIMARY KEY, name VARCHAR(50), grade CHAR(1), joined DATETIME);" > .github/migration/customer_table.sql || true
          echo "CREATE TABLE IF NOT EXISTS transactions (id INT PRIMARY KEY, customer_id INT, amount DECIMAL(16,2), date DATETIME);" > .github/migration/transaction_table.sql || true
          echo "[ECHO] migration SQL 생성" > .github/migration/echo_migration_sql_${TS}.txt || true

      - name: 📦 Node.js/JS/TS/Java/SpringBoot 설치/업그레이드/echo/fallback
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          sudo apt-get update -y | tee .github/echo_upgrade/echo_apt_update_${TS}.txt || true
          sudo apt-get install -y curl gnupg2 zip unzip wget | tee .github/echo_upgrade/echo_apt_install_base_${TS}.txt || true
          echo "[ECHO] Node.js 설치 시작 ($TS)" > .github/echo_upgrade/echo_nodejs_begin_${TS}.txt || true
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - | tee .github/echo_upgrade/echo_nodesource_${TS}.txt || true
          sudo apt-get install -y nodejs | tee .github/echo_upgrade/echo_nodejs_install_${TS}.txt || true
          sudo npm install -g npm@latest | tee .github/echo_upgrade/echo_npm_latest_${TS}.txt || true
          sudo npm install -g typescript@latest | tee .github/echo_upgrade/echo_typescript_latest_${TS}.txt || true
          node -v | tee .github/echo_upgrade/node_version_${TS}.txt || true
          npm -v  | tee .github/echo_upgrade/npm_version_${TS}.txt || true
          tsc -v | tee .github/echo_upgrade/tsc_version_${TS}.txt || true
          sudo apt-get install -y openjdk-17-jdk | tee .github/echo_upgrade/echo_openjdk_install_${TS}.txt || true
          java -version 2>&1 | tee .github/echo_upgrade/java_version_${TS}.txt || true
          sudo apt-get install -y maven | tee .github/echo_upgrade/echo_maven_install_${TS}.txt || true
          mvn -version | tee .github/echo_upgrade/mvn_version_${TS}.txt || true

          # Spring Boot CLI: 실패해도 중단 금지
          set +e
          SPRINGBOOT_VER=$(curl -s https://api.github.com/repos/spring-projects/spring-boot/releases/latest | grep tag_name | cut -d'"' -f4 | tr -d 'v')
          echo "SPRINGBOOT_VER=$SPRINGBOOT_VER" | tee .github/echo_upgrade/springboot_version_${TS}.txt || true
          wget -q "https://repo.spring.io/release/org/springframework/boot/spring-boot-cli/${SPRINGBOOT_VER}/spring-boot-cli-${SPRINGBOOT_VER}-bin.zip" -O /tmp/springboot.zip
          CLI_UNZIP=0
          if [ -s /tmp/springboot.zip ]; then
            UNZIP_OUT=$(unzip -o /tmp/springboot.zip -d /tmp/springboot_cli 2>&1)
            sudo cp -a /tmp/springboot_cli/spring-boot-cli-*/* /usr/local/ || true
            ln -sf /usr/local/bin/spring /usr/local/bin/springboot || true
            spring --version | tee .github/echo_upgrade/springboot_cli_version_${TS}.txt || true
            echo "[UPGRADE] Spring Boot CLI 정상 설치/업그레이드" | tee .github/echo_upgrade/springboot_cli_success_${TS}.txt || true
          else
            CLI_UNZIP=1
            echo "[ERROR] Spring Boot CLI 다운로드 실패, gradle 빌드로 fallback" | tee .github/echo_upgrade/springboot_cli_error_${TS}.txt || true
            echo "$UNZIP_OUT" | tee .github/echo_upgrade/springboot_cli_unzip_error_${TS}.txt || true
          fi
          set +e

          echo "[UPGRADE] 모든 언어/빌드툴/SpringBoot 최신화 완료" | tee .github/echo_upgrade/all_done_${TS}.txt || true

          for f in node npm tsc java mvn spring; do
            BIN_PATH=$(which $f 2>/dev/null)
            if [ -z "$BIN_PATH" ]; then
              echo "[ECHO][WARN][$TS] $f 실행파일 찾을 수 없음" | tee -a .github/echo_upgrade/echo_bin_copy_${f}_${TS}.txt || true
            else
              echo "[ECHO] $f 경로: $BIN_PATH" | tee -a .github/echo_upgrade/executables_${TS}.txt || true
              cp -a $BIN_PATH .github/ts_bin/ || echo "[ECHO] $f ts_bin 복사 실패" | tee -a .github/echo_upgrade/echo_bin_copy_${f}_${TS}.txt
              cp -a $BIN_PATH .github/mvn_install/ || echo "[ECHO] $f mvn_install 복사 실패" | tee -a .github/echo_upgrade/echo_bin_copy_${f}_${TS}.txt
              cp -a $BIN_PATH .github/springboot_projects/ || echo "[ECHO] $f springboot_projects 복사 실패" | tee -a .github/echo_upgrade/echo_bin_copy_${f}_${TS}.txt
              echo "[ECHO] $f 복사 완료" | tee -a .github/echo_upgrade/echo_bin_copy_${f}_${TS}.txt || true
            fi
          done
          if [ "$CLI_UNZIP" = "1" ]; then
            echo "[ECHO][ERROR][$TS] Spring Boot CLI 설치 완전 실패 - gradle 빌드 fallback" | tee -a .github/echo_upgrade/springboot_cli_final_error_${TS}.txt || true
          fi

      - name: 📄 샘플 JS/TS/Java/Spring 파일/설정 echo
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          echo "console.log('Hello, TypeScript!')" > .github/ts_projects/hello.ts || true
          echo "[ECHO] hello.ts 생성 ($TS)" > .github/echo_ts/echo_hello_ts_${TS}.txt || true
          echo "export function greet(name: string) { return 'Hello, ' + name + '!'; }" > .github/ts_projects/greet.ts || true
          echo "[ECHO] greet.ts 생성 ($TS)" > .github/echo_ts/echo_greet_ts_${TS}.txt || true
          echo "import { greet } from './greet'; console.log(greet('SpringBoot+MultiModule 자동화!'));" > .github/ts_projects/main.ts || true
          echo "[ECHO] main.ts 생성 ($TS)" > .github/echo_ts/echo_main_ts_${TS}.txt || true
          echo "{" > .github/ts_projects/tsconfig.json || true
          echo "  \"compilerOptions\": {" >> .github/ts_projects/tsconfig.json || true
          echo "    \"target\": \"es6\"," >> .github/ts_projects/tsconfig.json || true
          echo "    \"module\": \"commonjs\"," >> .github/ts_projects/tsconfig.json || true
          echo "    \"outDir\": \"./dist\"," >> .github/ts_projects/tsconfig.json || true
          echo "    \"strict\": true," >> .github/ts_projects/tsconfig.json || true
          echo "    \"esModuleInterop\": true" >> .github/ts_projects/tsconfig.json || true
          echo "  }," >> .github/ts_projects/tsconfig.json || true
          echo "  \"include\": [\"*.ts\"]" >> .github/ts_projects/tsconfig.json || true
          echo "}" >> .github/ts_projects/tsconfig.json || true
          echo "[ECHO] tsconfig.json 생성 ($TS)" > .github/echo_ts/echo_tsconfig_json_${TS}.txt || true
          echo "console.log('Hello, JavaScript! (SpringBoot+MultiModule)');" > .github/js_projects/hello.js || true
          echo "[ECHO] hello.js 생성 ($TS)" > .github/echo_js/echo_hello_js_${TS}.txt || true
          echo "function sum(a, b) { return a+b; } console.log('Sum:', sum(8,12));" > .github/js_projects/sum.js || true
          echo "[ECHO] sum.js 생성 ($TS)" > .github/echo_js/echo_sum_js_${TS}.txt || true
          mkdir -p .github/java_projects/src/main/java/com/fin/example || true
          echo "package com.fin.example; public class Main { public static void main(String[] args) { System.out.println(\"금융 Java 빌드(SpringBoot, 멀티모듈)\"); }}" > .github/java_projects/src/main/java/com/fin/example/Main.java || true
          echo "[ECHO] Main.java 생성 ($TS)" > .github/echo_java/echo_Main_java_${TS}.txt || true
          echo '<project xmlns="http://maven.apache.org/POM/4.0.0" ... </project>' > .github/java_projects/pom.xml || true
          echo "[ECHO] pom.xml 생성 ($TS)" > .github/echo_java/echo_pom_xml_${TS}.txt || true
          mkdir -p .github/springboot_projects/src/main/java/com/fin/springboot || true
          echo "package com.fin.springboot; import org.springframework.boot.SpringApplication; import org.springframework.boot.autoconfigure.SpringBootApplication; @SpringBootApplication public class Application { public static void main(String[] args) { SpringApplication.run(Application.class, args); }}" > .github/springboot_projects/src/main/java/com/fin/springboot/Application.java || true
          echo "[ECHO] Application.java 생성 ($TS)" > .github/echo_spring/echo_Application_java_${TS}.txt || true
          echo 'plugins { id "org.springframework.boot" version "3.2.6" } ...' > .github/springboot_projects/build.gradle || true
          echo "[ECHO] build.gradle 생성 ($TS)" > .github/echo_spring/echo_build_gradle_${TS}.txt || true
          echo "plugins { id 'org.springframework.boot' version '3.2.6' }" > .github/springboot_multi/app-api/build.gradle || true
          echo "[ECHO] app-api build.gradle 생성 ($TS)" > .github/echo_multimodule/echo_appapi_build_gradle_${TS}.txt || true
          echo "plugins { id 'java' }" > .github/springboot_multi/app-core/build.gradle || true
          echo "[ECHO] app-core build.gradle 생성 ($TS)" > .github/echo_multimodule/echo_appcore_build_gradle_${TS}.txt || true
          echo "plugins { id 'java' }" > .github/springboot_multi/app-web/build.gradle || true
          echo "[ECHO] app-web build.gradle 생성 ($TS)" > .github/echo_multimodule/echo_appweb_build_gradle_${TS}.txt || true

      - name: 🛠️ TypeScript 빌드/실행/에코/백업
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          cd .github/ts_projects || true
          echo "[ECHO] TypeScript 컴파일 시작 ($TS)" > ../echo_compile/echo_tsc_compile_begin_${TS}.txt || true
          tsc -p tsconfig.json | tee ../echo_compile/tsc_compile_${TS}.log || echo "[ECHO] tsc 컴파일 실패 ($TS)" >> ../echo_compile/echo_tsc_compile_begin_${TS}.txt
          echo "[ECHO] TypeScript 컴파일 완료 ($TS)" > ../echo_compile/echo_tsc_compile_end_${TS}.txt || true
          ls -l dist > ../echo_compile/ts_dist_list_${TS}.txt || true
          echo "[ECHO] TypeScript dist 목록 저장 ($TS)" > ../echo_compile/echo_tsc_dist_list_${TS}.txt || true
          node dist/main.js | tee ../echo_run/ts_run_${TS}.log || echo "[ECHO] node 실행 실패 ($TS)" >> ../echo_run/ts_run_${TS}.log
          echo "[ECHO] TypeScript 실행 완료 ($TS)" > ../echo_run/echo_tsc_run_${TS}.txt || true
          cd ../.. || true
          cp -a .github/ts_projects/dist/. .github/ts_dist_backup/ || echo "[ECHO] ts_dist_backup 복사 실패 ($TS)" > .github/echo_compile/echo_ts_dist_backup_${TS}.txt
          echo "[ECHO] ts_dist_backup 복사 완료 ($TS)" > .github/echo_compile/echo_ts_dist_backup_${TS}.txt || true
          cp -a .github/ts_projects/*.ts .github/ts_projects/tsconfig.json .github/ts_backup/ || echo "[ECHO] ts_backup 복사 실패 ($TS)" > .github/echo_compile/echo_ts_backup_${TS}.txt
          echo "[ECHO] ts_backup 복사 완료 ($TS)" > .github/echo_compile/echo_ts_backup_${TS}.txt || true
          echo "[에코] TypeScript 빌드/실행/백업/업그레이드 완료" >> .github/echo_result/ts_result_${TS}.txt || true

      - name: 🛠️ JavaScript 실행/에코/백업
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          node .github/js_projects/hello.js | tee .github/echo_run/js_hello_${TS}.log || echo "[ECHO] hello.js 실행 실패 ($TS)" >> .github/echo_run/js_hello_${TS}.log
          echo "[ECHO] hello.js 실행 완료 ($TS)" > .github/echo_run/echo_hello_js_run_${TS}.txt || true
          node .github/js_projects/sum.js | tee .github/echo_run/js_sum_${TS}.log || echo "[ECHO] sum.js 실행 실패 ($TS)" >> .github/echo_run/js_sum_${TS}.log
          echo "[ECHO] sum.js 실행 완료 ($TS)" > .github/echo_run/echo_sum_js_run_${TS}.txt || true
          cp -a .github/js_projects/*.js .github/js_backup/ || echo "[ECHO] js_backup 복사 실패 ($TS)" > .github/echo_run/echo_js_backup_${TS}.txt
          echo "[ECHO] js_backup 복사 완료 ($TS)" > .github/echo_run/echo_js_backup_${TS}.txt || true
          echo "[에코] JavaScript 실행/백업/업그레이드 완료" >> .github/echo_result/js_result_${TS}.txt || true

      - name: 🛠️ Maven(Java) 빌드/실행/에코/백업
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          cd .github/java_projects || true
          echo "[ECHO] Maven 컴파일 시작 ($TS)" > ../echo_compile/echo_mvn_compile_begin_${TS}.txt || true
          mvn clean compile | tee ../echo_compile/mvn_compile_${TS}.log || echo "[ECHO] maven 컴파일 실패 ($TS)" >> ../echo_compile/mvn_compile_${TS}.log
          echo "[ECHO] Maven 컴파일 완료 ($TS)" > ../echo_compile/echo_mvn_compile_end_${TS}.txt || true
          mkdir -p ../java_build/ || true
          cp -a target ../java_build/ || echo "[ECHO] java_build 복사 실패 ($TS)" > ../echo_compile/echo_java_build_${TS}.txt
          echo "[ECHO] java_build 복사 완료 ($TS)" > ../echo_compile/echo_java_build_${TS}.txt || true
          cd ../.. || true
          echo "[에코] Maven(Java) 빌드/백업/업그레이드 완료" >> .github/echo_result/java_result_${TS}.txt || true

      - name: 🛠️ SpringBoot 빌드/실행/에코/백업
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          cd .github/springboot_projects || true
          echo "[ECHO] SpringBoot 빌드 시작 ($TS)" > ../springboot_logs/echo_gradle_build_begin_${TS}.txt || true
          if [ ! -f ./gradlew ]; then
            gradle build | tee ../springboot_logs/gradle_build_${TS}.log || echo "[ECHO] gradle 빌드 실패 ($TS)" >> ../springboot_logs/gradle_build_${TS}.log
          else
            ./gradlew build | tee ../springboot_logs/gradle_build_${TS}.log || echo "[ECHO] gradlew 빌드 실패 ($TS)" >> ../springboot_logs/gradle_build_${TS}.log
          fi
          echo "[ECHO] SpringBoot 빌드 완료 ($TS)" > ../springboot_logs/echo_gradle_build_end_${TS}.txt || true
          cp -a build ../springboot_build/ || echo "[ECHO] springboot_build 복사 실패 ($TS)" > ../springboot_logs/echo_springboot_build_${TS}.txt
          echo "[ECHO] springboot_build 복사 완료 ($TS)" > ../springboot_logs/echo_springboot_build_${TS}.txt || true
          cd ../.. || true
          cp -a .github/springboot_projects/* .github/springboot_backup/ || echo "[ECHO] springboot_backup 복사 실패 ($TS)" > .github/springboot_logs/echo_springboot_backup_${TS}.txt
          echo "[ECHO] springboot_backup 복사 완료 ($TS)" > .github/springboot_logs/echo_springboot_backup_${TS}.txt || true
          echo "[에코] SpringBoot 빌드/백업/업그레이드 완료" >> .github/echo_result/springboot_result_${TS}.txt || true

      - name: 🗂️ 빌드/실행/로그/백업/이력 통합 echo
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          find .github/ -type f | tee .github/build_logs/files_all_${TS}.log || true
          echo "[ECHO] files_all 로그 저장 ($TS)" > .github/build_logs/echo_files_all_${TS}.txt || true
          ls -ld .github/* | tee -a .github/build_logs/perm_owner_${TS}.log || true
          echo "[ECHO] perm_owner 로그 저장 ($TS)" > .github/build_logs/echo_perm_owner_${TS}.txt || true
          echo "[에코] 전체 빌드/실행/이력/로그/백업/업그레이드 파일리스트 출력완료" >> .github/echo_result/total_result_${TS}.txt || true

      - name: 🕓 빌드/업그레이드 이력(날짜별, 커밋별) 기록 echo
        run: |
          set +e
          TS=$(date +%Y%m%d_%H%M%S)
          echo "빌드시간: $(date '+%Y-%m-%d %H:%M:%S')" > .github/build_history/last_build_time.txt || true
          echo "커밋 SHA: $GITHUB_SHA" > .github/build_history/last_build_commit.txt || true
          echo "[ECHO] last_build_time.txt, last_build_commit.txt 생성 ($TS)" > .github/build_history/echo_last_build_${TS}.txt || true
          echo "[에코] 빌드/업그레이드 이력 파일 생성" >> .github/echo_result/total_result_${TS}.txt || true

      - name: 📦 전체 빌드/실행/로그/이력 artifact 저장 echo
        uses: actions/upload-artifact@v4
        with:
          name: finance-build-all-artifacts
          path: |
            .github/ts_dist_backup/
            .github/ts_backup/
            .github/js_backup/
            .github/java_build/
            .github/springboot_build/
            .github/springboot_backup/
            .github/build_logs/
            .github/build_history/
            .github/echo_compile/
            .github/echo_build/
            .github/echo_run/
            .github/echo_result/
            .github/echo_upgrade/
            .github/echo_spring/
            .github/springboot_logs/
            .github/echo_env/
            .github/echo_secret/
            .github/echo_buildopt/
            .github/echo_multimodule/
            .github/echo_ts/
            .github/echo_js/
            .github/echo_java/
            .github/migration/
