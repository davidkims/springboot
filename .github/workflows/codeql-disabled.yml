name: 🚫 CodeQL Scan Disabled Workflow

# ✅ 트리거 제거: 수동 실행만 허용
on:
  workflow_dispatch:

jobs:
  disable-codeql:
    name: ❌ Disable CodeQL Auto Scan
    runs-on: ubuntu-latest
    steps:
      - name: 🧼 Checkout
        uses: actions/checkout@v4

      - name: 🔍 Skipping CodeQL Analysis
        run: |
          echo "[🚫] CodeQL 자동 분석을 비활성화했습니다."
          echo "[ℹ️] 이 워크플로우는 수동으로 실행할 때만 동작하며 Code Scanning 이벤트를 발생시키지 않습니다."

      # ❌ CodeQL 분석 제거
      # - name: Initialize CodeQL
      #   uses: github/codeql-action/init@v3
      #   with:
      #     languages: [ 'java', 'python' ]  # 언어 설정 필요시

      # ❌ CodeQL 분석 제거
      # - name: Perform CodeQL Analysis
      #   uses: github/codeql-action/analyze@v3

      # ❌ SARIF 업로드 제거
      # - name: Upload SARIF
      #   uses: github/codeql-action/upload-sarif@v3
      #   with:
      #     sarif_file: results.sarif
