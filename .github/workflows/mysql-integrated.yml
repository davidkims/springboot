name: 🐬 MySQL Full Setup, Migration & Auto Backup

on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  mysql_full_pipeline:
    runs-on: ubuntu-latest

    env:
      MYSQL_IMAGE: mysql:8.0
      DB_NAME: example_db
      DB_USER: root
      DB_PASS: rootpass123
      ROOT_PASS: rootpass123
      MIGRATION_DIR: /opt/mysql/migrations
      BACKUP_DIR: /opt/backup/sql

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 📁 디렉토리 준비
        run: |
          echo "[+] Creating directories..."
          sudo mkdir -p "${MIGRATION_DIR}" "${BACKUP_DIR}"
          echo "[+] Setting permissions..."
          sudo chmod -R 777 /opt/mysql /opt/backup

      - name: 🧾 SQL 마이그레이션 파일 생성
        run: |
          echo "[+] Creating SQL migration file..."
          echo "
          CREATE TABLE IF NOT EXISTS accounts (
              id INT AUTO_INCREMENT PRIMARY KEY,
              name VARCHAR(50),
              balance DECIMAL(12,2)
          );
          CREATE TABLE IF NOT EXISTS corporate_transactions (
              transaction_id INT AUTO_INCREMENT PRIMARY KEY,
              corporate_name VARCHAR(100) NOT NULL,
              transaction_type VARCHAR(50) NOT NULL,
              amount DECIMAL(12,2) NOT NULL,
              transaction_date DATETIME DEFAULT CURRENT_TIMESTAMP
          );
          INSERT INTO accounts(name, balance) VALUES ('홍길동', 1000000);
          INSERT INTO corporate_transactions(corporate_name, transaction_type, amount) VALUES ('ABC Corporation', 'Purchase', 500000);
          INSERT INTO corporate_transactions(corporate_name, transaction_type, amount) VALUES ('XYZ Inc.', 'Sale', 250000);
          " | sudo tee "${MIGRATION_DIR}/migrate.sql"

      - name: 🧪 MySQL 서버 설치
        run: |
          echo "[+] Installing MySQL server..."
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y mysql-server || { echo "❌ MySQL 설치 실패"; exit 1; }

      - name: 🔐 루트 비밀번호 재설정 및 MySQL 안전 모드
        run: |
          echo "[+] Resetting root password in safe mode..."
          sudo systemctl stop mysql
          echo "[+] Preparing MySQL runtime..."
          sudo mkdir -p /var/run/mysqld
          sudo chown mysql:mysql /var/run/mysqld
          echo "[+] Starting MySQL in safe mode..."
          sudo mysqld_safe --skip-networking --skip-grant-tables &
          sleep 10
          echo "[+] Changing root password..."
          echo "FLUSH PRIVILEGES; ALTER USER 'root'@'localhost' IDENTIFIED BY '${{ env.ROOT_PASS }}'; FLUSH PRIVILEGES;" | sudo mysql -u root
          echo "[+] Stopping safe mode..."
          sudo killall mysqld
          sleep 5
          echo "[+] Restarting MySQL service..."
          sudo systemctl start mysql || { echo "❌ MySQL 시작 실패"; exit 1; }

      - name: 🛠️ 데이터베이스 및 사용자 설정
        run: |
          echo "[+] Creating database and user..."
          mysql -u root -p"${{ env.ROOT_PASS }}" -e "CREATE DATABASE IF NOT EXISTS ${{ env.DB_NAME }};"
          echo "[+] Creating user if not exists..."
          mysql -u root -p"${{ env.ROOT_PASS }}" -e "CREATE USER IF NOT EXISTS '${{ env.DB_USER }}'@'localhost' IDENTIFIED BY '${{ env.DB_PASS }}';"
          echo "[+] Granting privileges..."
          mysql -u root -p"${{ env.ROOT_PASS }}" -e "GRANT ALL PRIVILEGES ON ${{ env.DB_NAME }}.* TO '${{ env.DB_USER }}'@'localhost';"
          mysql -u root -p"${{ env.ROOT_PASS }}" -e "FLUSH PRIVILEGES;"

      - name: 📥 마이그레이션 실행
        run: |
          echo "[+] Running migration..."
          mysql -u "${{ env.DB_USER }}" -p"${{ env.DB_PASS }}" "${{ env.DB_NAME }}" < "${MIGRATION_DIR}/migrate.sql" || { echo "❌ 마이그레이션 실패"; exit 1; }

      - name: ✅ 데이터 확인
        run: |
          echo "[+] Verifying data..."
          mysql -u "${{ env.DB_USER }}" -p"${{ env.DB_PASS }}" -e "USE ${{ env.DB_NAME }}; SHOW TABLES; SELECT * FROM accounts; SELECT * FROM corporate_transactions;"

      - name: 🕒 타임스탬프 및 포트 생성
        id: vars
        run: |
          echo "[+] Generating timestamp and random port..."
          echo "timestamp=$(date +%Y%m%d%H%M%S)" >> "$GITHUB_OUTPUT"
          echo "container=backup-mysql-$(date +%Y%m%d%H%M%S)" >> "$GITHUB_OUTPUT"
          echo "host_port=$((RANDOM%1000+3307))" >> "$GITHUB_OUTPUT"

      - name: 🐳 MySQL 백업 컨테이너 실행
        run: |
          echo "[+] Launching MySQL backup container..."
          docker run -d \
            --name "${{ steps.vars.outputs.container }}" \
            -e MYSQL_ROOT_PASSWORD="${{ env.DB_PASS }}" \
            -e MYSQL_DATABASE="${{ env.DB_NAME }}" \
            -p "${{ steps.vars.outputs.host_port }}:3306" \
            "${{ env.MYSQL_IMAGE }}"
          sleep 30 || { echo "❌ 백업 컨테이너 시작 실패"; exit 1; }

      - name: 🔐 .my.cnf 생성
        run: |
          echo "[+] Creating .my.cnf file..."
          echo "[client]" > ~/.my.cnf
          echo "user=${{ env.DB_USER }}" >> ~/.my.cnf
          echo "password=${{ env.DB_PASS }}" >> ~/.my.cnf
          echo "host=127.0.0.1" >> ~/.my.cnf
          echo "port=${{ steps.vars.outputs.host_port }}" >> ~/.my.cnf
          chmod 600 ~/.my.cnf

      - name: 💾 mysqldump 백업 및 압축
        run: |
          echo "[+] Starting backup..."
          TIMESTAMP="${{ steps.vars.outputs.timestamp }}"
          BACKUP_FILE="${{ env.BACKUP_DIR }}/backup-${TIMESTAMP}.sql"
          ARCHIVE_FILE="${BACKUP_FILE}.gz"
          mysqldump "${{ env.DB_NAME }}" > "${BACKUP_FILE}" || { echo "❌ 백업 실패"; exit 1; }
          echo "[+] Compressing backup..."
          gzip -f "${BACKUP_FILE}"
          echo "✅ 백업 및 압축 완료: ${ARCHIVE_FILE}"
          ls -lh "${{ env.BACKUP_DIR }}"

      - name: 🔍 컨테이너 정보 출력
        run: |
          echo "📦 컨테이너 이름: ${{ steps.vars.outputs
