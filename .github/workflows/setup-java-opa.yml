name: ☕ Java + 🛡️ 사용자별 정책 디렉토리 자동화 (동적 반복)

on:
  workflow_dispatch:

jobs:
  opa-user-policy-directory:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 저장소 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 설치
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🔧 OPA 설치
        env:
          OPA_VERSION: "v0.63.0"
        run: |
          echo "[🛠️] OPA 설치 시작 - 버전: $OPA_VERSION"
          curl -sSL -o opa https://openpolicyagent.org/downloads/${OPA_VERSION}/opa_linux_amd64_static
          chmod +x opa
          sudo mv opa /usr/local/bin/opa
          echo "[✅] OPA 설치 완료"
          opa version

      - name: 📁 정책 파일 생성
        run: |
          echo "[📁] opa_policies 디렉토리 생성"
          mkdir -p opa_policies

          echo "[📄] allow.rego 생성"
          echo 'package example' | tee opa_policies/allow.rego
          echo '' | tee -a opa_policies/allow.rego
          echo 'default allow = false' | tee -a opa_policies/allow.rego
          echo '' | tee -a opa_policies/allow.rego
          echo 'allow {' | tee -a opa_policies/allow.rego
          echo '  input.user == "admin"' | tee -a opa_policies/allow.rego
          echo '}' | tee -a opa_policies/allow.rego

          echo "[📄] deny.rego 생성"
          echo 'package example' | tee opa_policies/deny.rego
          echo '' | tee -a opa_policies/deny.rego
          echo 'default deny = true' | tee -a opa_policies/deny.rego
          echo '' | tee -a opa_policies/deny.rego
          echo 'deny {' | tee -a opa_policies/deny.rego
          echo '  input.user == "admin"' | tee -a opa_policies/deny.rego
          echo '}' | tee -a opa_policies/deny.rego

          echo "[📄] role.rego 생성"
          echo 'package example' | tee opa_policies/role.rego
          echo '' | tee -a opa_policies/role.rego
          echo 'role = input.role' | tee -a opa_policies/role.rego

      - name: 📂 사용자별 정책 디렉토리 동적 생성 및 복사
        run: |
          echo "[🧑] 사용자 목록 정의"
          users=(admin guest manager auditor)

          echo "[📂] site/policies/user 디렉토리 생성"
          mkdir -p site/policies/user

          for user in "${users[@]}"; do
            echo "[📁] 사용자 디렉토리 생성: $user"
            mkdir -p "site/policies/user/$user"

            echo "[📥] 정책 파일 복사 → site/policies/user/$user/"
            cp opa_policies/allow.rego "site/policies/user/$user/allow.rego"
            cp opa_policies/deny.rego "site/policies/user/$user/deny.rego"
            cp opa_policies/role.rego "site/policies/user/$user/role.rego"
          done

          echo "[✅] 사용자별 정책 디렉토리 구조 복사 완료"
          echo "[📂] 최종 구조 확인:"
          tree site/policies || ls -R site/policies

      - name: ✅ OPA 유닛 테스트 실행
        run: |
          echo "[🚀] opa test 실행 시작..."
          opa test opa_policies/
          echo "[✅] opa test 완료"

      - name: ℹ️ Java & OPA 버전 확인
        run: |
          echo "[ℹ️] Java 버전:"
          java -version
          echo "[ℹ️] OPA 버전:"
          opa version
