name: 🧾 Multi-Transaction Backup (Per-Type Containers)

on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  finance:
    runs-on: ubuntu-latest

    env:
      ROOT_PASS: rootpass123
      SQL_DIR: /opt/finance/sql
      BACKUP_DIR: /opt/finance/backups
      TYPES: |
        bank_transfer
        check_card
        credit_card
        bitcoin
        stock
        futures

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 📁 Prepare Directories & SQL
        run: |
          echo "[📁] Creating required directories..."
          sudo mkdir -p $SQL_DIR $BACKUP_DIR
          sudo chmod -R 777 /opt/finance

          echo "[📝] Generating init.sql..."
          echo "CREATE TABLE IF NOT EXISTS transfers (
            id INT AUTO_INCREMENT PRIMARY KEY,
            type VARCHAR(30),
            amount DECIMAL(15,2),
            currency VARCHAR(5),
            status VARCHAR(20)
          );" > $SQL_DIR/init.sql

          for TYPE in $TYPES; do
            echo "-- Inserts for $TYPE" > $SQL_DIR/${TYPE}_insert.sql
            for i in $(seq 1 100); do
              AMOUNT=$(( (RANDOM % 1000000) + 1000 ))
              STATUS=$([ $((RANDOM % 2)) -eq 0 ] && echo "completed" || echo "pending")
              echo "INSERT INTO transfers(type, amount, currency, status) VALUES('$TYPE', $AMOUNT, 'KRW', '$STATUS');" >> $SQL_DIR/${TYPE}_insert.sql
            done
            cat $SQL_DIR/${TYPE}_insert.sql >> $SQL_DIR/init.sql
          done
          echo "[✔] SQL 준비 완료"

      - name: 🔁 Loop through types, launch MySQL, and backup
        run: |
          for TYPE in $TYPES; do
            DB_NAME="db_${TYPE}"
            DB_USER="user_${TYPE}"
            DB_PASS="pass_${TYPE}"
            CONTAINER="mysql_${TYPE}"
            PORT=$((3307 + RANDOM % 1000))

            echo "🚀 Starting $CONTAINER for $TYPE on port $PORT"
            docker run -d \
              --name $CONTAINER \
              -e MYSQL_ROOT_PASSWORD=$ROOT_PASS \
              -e MYSQL_DATABASE=$DB_NAME \
              -e MYSQL_USER=$DB_USER \
              -e MYSQL_PASSWORD=$DB_PASS \
              -v $SQL_DIR:/docker-entrypoint-initdb.d \
              -p $PORT:3306 \
              --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -p$ROOT_PASS --silent" \
              --health-interval=5s \
              --health-timeout=3s \
              --health-retries=10 \
              mysql:8.0

            echo "⏳ Waiting for $CONTAINER to become healthy..."
            for i in {1..10}; do
              if ! docker ps -a --format '{{.Names}}' | grep -q "^$CONTAINER$"; then
                echo "[❌] Container $CONTAINER does not exist (may have crashed)."
                exit 1
              fi

              STATUS=$(docker inspect --format='{{.State.Health.Status}}' $CONTAINER || echo "notfound")
              echo "  - Health check $i: $STATUS"
              if [ "$STATUS" = "healthy" ]; then
                echo "[✔] $CONTAINER is healthy."
                break
              fi
              sleep 6
              if [ "$i" -eq 10 ]; then
                echo "[❌] $CONTAINER failed to become healthy after 10 tries."
                docker logs $CONTAINER
                docker stop $CONTAINER
                exit 1
              fi
            done

            echo "🔍 Querying $TYPE for completed transactions..."
            COUNT=$(mysql -h 127.0.0.1 -P $PORT -u$DB_USER -p$DB_PASS -N -e "USE $DB_NAME; SELECT COUNT(*) FROM transfers WHERE type='$TYPE' AND status='completed';")
            echo "✅ Found $COUNT completed transactions for $TYPE"

            if [ "$COUNT" != "0" ]; then
              FILE="$BACKUP_DIR/${TYPE}_backup.sql"
              echo "💾 Backing up $TYPE to $FILE"
              mysqldump --no-tablespaces -h 127.0.0.1 -P $PORT -u$DB_USER -p$DB_PASS $DB_NAME > $FILE
              echo "[✔] Backup saved for $TYPE"
            else
              echo "⏭️ No completed transactions for $TYPE. Skipping backup."
            fi

            echo "🛑 Stopping container $CONTAINER"
            docker stop $CONTAINER
          done

      - name: 🗜️ Compress Backups
        run: |
          echo "[🗜️] Compressing backups..."
          cd $BACKUP_DIR
          ARCHIVE_NAME="finance-backups-$(date +%Y%m%d%H%M%S).tar.gz"
          tar -czf $ARCHIVE_NAME *.sql
          echo "[✔] Archive created: $ARCHIVE_NAME"
