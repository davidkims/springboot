name: "🛡️ Echo 기반 보안 자동화 + 템플릿 + Placeholder 생성"

on:
  workflow_dispatch:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 2 * * 5'

jobs:
  full-echo-secure:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      security-events: write

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🧰 필수 서비스 설치 (scorecard, gh, jq, yq, codeql binary)
        run: |
          echo "[Install] 필수 도구 설치..."
          sudo apt update -y
          sudo apt install -y curl jq yq tree lsof sudo ssh python3-pip

          echo "[Install] GitHub CLI 설치"
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo tee /usr/share/keyrings/githubcli-archive-keyring.gpg >/dev/null
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list
          sudo apt update && sudo apt install gh -y

          echo "[Install] OSSF Scorecard 설치"
          SCORECARD_VERSION=4.10.3
          curl -LJO "https://github.com/ossf/scorecard/releases/download/v${SCORECARD_VERSION}/scorecard_${SCORECARD_VERSION}_linux_amd64"
          chmod +x scorecard_${SCORECARD_VERSION}_linux_amd64
          sudo mv scorecard_${SCORECARD_VERSION}_linux_amd64 /usr/local/bin/scorecard
          scorecard --version

          echo "[Install] GitHub CodeQL CLI 설치"
          CODEQL_VERSION=2.17.5
          curl -L -o codeql.zip "https://github.com/github/codeql-cli-binaries/releases/download/v${CODEQL_VERSION}/codeql-linux64.zip"
          unzip codeql.zip
          sudo mv codeql/codeql /usr/local/bin/codeql
          codeql version

      - name: 📁 Echo 디렉토리 및 권한 Snapshot
        run: |
          for d in .github/echo_{template,security,codeql,dependabot,permission,placeholder}; do mkdir -p "$d"; done
          whoami > .github/echo_permission/whoami.txt
          id > .github/echo_permission/id.txt
          cat /etc/passwd > .github/echo_permission/passwd.txt
          sudo cat /etc/sudoers > .github/echo_permission/sudoers.txt
          getent group sudo > .github/echo_permission/sudo_group.txt
          sudo -l > .github/echo_permission/sudo_l.txt

      - name: 🧾 .env/.yml/.cfg 템플릿 + echo 파일 생성
        run: |
          echo "DB_HOST=PLACEHOLDER_HOST" > .github/echo_template/sample.env
          echo "version: '3'\nservices:\n  app:\n    image: PLACEHOLDER_IMAGE" > .github/echo_template/sample.yml
          echo "[config]\nname=PLACEHOLDER_NAME" > .github/echo_template/sample.cfg
          echo "echo 'DB_HOST=PLACEHOLDER_HOST'" > .github/echo_template/echo_env.sh
          echo "echo 'version: ... PLACEHOLDER_IMAGE'" > .github/echo_template/echo_yml.sh
          echo "echo '[config] ... PLACEHOLDER_NAME'" > .github/echo_template/echo_cfg.sh

      - name: 🛡️ 보안 템플릿 자동 생성 (CodeQL/Dependabot/SECURITY)
        run: |
          echo -e "name: CodeQL\non:\n  push:\n    branches: [\"main\"]\njobs:\n  analyze:\n    runs-on: ubuntu-latest\n    steps:\n    - uses: actions/checkout@v4\n    - uses: github/codeql-action/init@v3\n      with:\n        languages: javascript\n    - uses: github/codeql-action/analyze@v3" > .github/codeql.yml
          echo -e "version: 2\nupdates:\n  - package-ecosystem: \"npm\"\n    directory: \"/\"\n    schedule:\n      interval: \"daily\"" > .github/dependabot.yml
          echo "# Security Policy\nPlease report vulnerabilities to: security@PLACEHOLDER_DOMAIN" > .github/SECURITY.md

      - name: 🧪 자동 Placeholder 파일(.sh/.sql/.md/.json/.rs/.py/.js) 생성
        run: |
          echo "#!/bin/bash" > .github/echo_placeholder/echo_placeholder.sh
          echo "echo 'PLACEHOLDER_SCRIPT'" >> .github/echo_placeholder/echo_placeholder.sh
          echo "-- PLACEHOLDER SQL" > .github/echo_placeholder/sample.sql
          echo "echo '-- PLACEHOLDER SQL'" > .github/echo_placeholder/echo_sql.sh
          echo "# PLACEHOLDER Doc" > .github/echo_placeholder/sample.md
          echo "echo '# PLACEHOLDER Doc'" > .github/echo_placeholder/echo_md.sh
          echo '{ "key": "PLACEHOLDER" }' > .github/echo_placeholder/sample.json
          echo "echo '{ \"key\": \"PLACEHOLDER\" }'" > .github/echo_placeholder/echo_json.sh
          echo 'fn main() { println!("PLACEHOLDER"); }' > .github/echo_placeholder/sample.rs
          echo 'print("PLACEHOLDER")' > .github/echo_placeholder/sample.py
          echo 'console.log("PLACEHOLDER")' > .github/echo_placeholder/sample.js

      - name: 📊 Scorecard 분석 결과 echo 생성
        run: |
          echo "echo '[Scorecard] 실행'" > .github/echo_security/echo_scorecard.sh
          echo "scorecard --repo=https://github.com/${{ github.repository }} --format=json > .github/echo_security/scorecard_result.json" >> .github/echo_security/echo_scorecard.sh
          scorecard --repo=https://github.com/${{ github.repository }} --format=json > .github/echo_security/scorecard_result.json

      - name: 📤 Upload All Echo & Template Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: full-echo-security-placeholder
          path: |
            .github/echo_*
            .github/codeql.yml
            .github/dependabot.yml
            .github/SECURITY.md
