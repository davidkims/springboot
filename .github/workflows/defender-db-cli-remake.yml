name: "🛰 Net Echo (Pre-Docker) — Echo + Always Commit + External Upload (No-Fail)"

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

env:
  ECHO_ROOT: .github/echo_net
  TARGET_REPO_OWNER: davidkims
  TARGET_REPO_NAME: springboot
  GH_ADMIN_TOKEN: ${{ secrets.GH_ADMIN_TOKEN }}

jobs:
  # ==================== Unix (Ubuntu + macOS) ====================
  net-echo-unix:
    name: "Unix — Echo (${{ matrix.os }})"
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-15, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - name: "[ECHO] Checkout"
        uses: actions/checkout@v4

      - name: "[ECHO] Runner migration notice (macOS)"
        if: startsWith(matrix.os, 'macos-') || matrix.os == 'macos-latest'
        shell: bash
        run: |
          echo "[ECHO] ⚠ macos-latest → macOS 15 마이그레이션(2025-08-04~). macos-14 종속 시 macos-14로 핀 권장."

      - name: "[ECHO] Prepare dirs"
        shell: bash
        run: |
          set -Eeuo pipefail
          mkdir -p "${ECHO_ROOT}/out" "${ECHO_ROOT}/bin" "${ECHO_ROOT}/logs"

      - name: "[ECHO] Install helpers (Linux only)"
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get -y update || true
          sudo apt-get -y install net-tools dnsutils iproute2 iptables nftables ethtool lshw || true

      - name: "[ECHO] Write Bash collector"
        shell: bash
        run: |
          set -Eeuo pipefail
          cat > "${ECHO_ROOT}/gather_net_echo.sh" <<'SH'
          #!/usr/bin/env bash
          set -Eeuo pipefail
          ECHO_ROOT="${ECHO_ROOT:-./net_echo}"
          OUT_DIR="${OUT_DIR:-${ECHO_ROOT}/out}"
          BIN_DIR="${BIN_DIR:-${ECHO_ROOT}/bin}"
          LOG_DIR="${LOG_DIR:-${ECHO_ROOT}/logs}"
          ARCHIVE="${ARCHIVE:-${ECHO_ROOT}/net_echo_$(date +%Y%m%d_%H%M%S).tar.gz}"
          mkdir -p "$OUT_DIR" "$BIN_DIR" "$LOG_DIR"
          echoe(){ echo "[ECHO][$(date +%H:%M:%S)] $*"; }
          trap 'rc=$?; echoe "FAILED at: ${BASH_COMMAND} (rc=${rc})"; exit $rc' ERR
          check_cmd(){ local c="$1"; if command -v "$c" >/dev/null 2>&1; then
            local v; v="$("$c" --version 2>/dev/null | head -n1 || true)"
            printf "%-18s : %s\n" "$c" "${v:-(no version line)}" | tee -a "${BIN_DIR}/commands.txt"
          else printf "%-18s : MISSING\n" "$c" | tee -a "${BIN_DIR}/commands.txt"; fi; }
          dump(){ local title="$1"; shift; local out="${OUT_DIR}/${title}.txt"
            echoe "[RUN] $* -> ${out}"; if "$@" >"${out}" 2>&1; then echoe "[OK] ${title}"; else echoe "[WARN] ${title}"; fi; }
          copy_file(){ local p="$1" n="${2:-$(basename "$1")}"; [ -f "$p" ] && { echoe "[COPY] $p -> ${OUT_DIR}/${n}"; cp -a "$p" "${OUT_DIR}/${n}"; } || echoe "[MISS] $p"; }

          { echo "DATE: $(date -Is)"; echo "UNAME: $(uname -a)";
            echo; echo "OS RELEASE:"; (cat /etc/os-release 2>/dev/null || sw_vers 2>/dev/null || true); } | tee "${OUT_DIR}/00_system_overview.txt"

          for c in ip ifconfig route ss netstat arp iptables ip6tables nft ufw resolvectl systemd-resolve nmcli networksetup scutil ethtool lshw lspci iw iwconfig iwlist nslookup dig host curl wget netplan sysctl; do check_cmd "$c" || true; done

          if command -v ip >/dev/null; then
            dump "ip_addr" ip addr; dump "ip_link" ip -d link; dump "ip_neigh" ip neigh
            dump "ip_route" ip route show table all; dump "ip_rule" ip rule show
          else command -v ifconfig >/dev/null && dump "ifconfig" ifconfig || true
               command -v route    >/dev/null && dump "route" route -n || true; fi

          if command -v ss >/dev/null; then dump "ss_tcp_listen" ss -tlnp; dump "ss_udp_listen" ss -ulnp; dump "ss_summary" ss -s
          elif command -v netstat >/dev/null; then dump "netstat_listen" netstat -tulpn; dump "netstat_routes" netstat -rn; fi

          command -v arp >/dev/null && dump "arp" arp -an || true
          copy_file "/etc/resolv.conf" "resolv.conf"; copy_file "/etc/hosts" "hosts"; copy_file "/etc/nsswitch.conf" "nsswitch.conf"
          command -v resolvectl >/dev/null && dump "resolvectl_status" resolvectl status || true
          command -v systemd-resolve >/dev/null && dump "systemd_resolve_status" systemd-resolve --status || true
          command -v ufw >/dev/null && dump "ufw_status" ufw status verbose || true
          command -v iptables  >/dev/null && dump "iptables_save"  sh -c 'iptables -S; iptables -L -v -n' || true
          command -v ip6tables >/dev/null && dump "ip6tables_save" sh -c 'ip6tables -S; ip6tables -L -v -n' || true
          command -v nft >/dev/null && dump "nft_ruleset" nft list ruleset || true

          dump "sysctl_net" sh -c 'sysctl -a 2>/dev/null | grep -E "^(net\.|fs\.ipv4|fs\.ipv6)" || true'
          command -v ethtool >/dev/null && { for nic in $(ls /sys/class/net 2>/dev/null | grep -v lo || true); do dump "ethtool_${nic}" ethtool "$nic"; done; }
          command -v lspci >/dev/null && dump "lspci_net"  sh -c 'lspci -nn | grep -i -E "net|ether|wireless|wifi"'
          command -v lshw  >/dev/null && dump "lshw_net"   sh -c 'lshw -class network -sanitize'
          command -v iw >/dev/null && dump "iw_link" iw dev || true
          command -v iwconfig >/dev/null && dump "iwconfig" iwconfig || true
          command -v iwlist   >/dev/null && dump "iwlist_scan" sh -c 'iwlist scan 2>/dev/null || true'

          command -v nmcli >/dev/null && { dump "nmcli_general" nmcli general status; dump "nmcli_dev" nmcli device status; dump "nmcli_conn" nmcli -f NAME,UUID,TYPE,DEVICE connection show; dump "nmcli_dns" nmcli dev show; }
          if [[ "$(uname -s)" == "Darwin" ]]; then
            command -v networksetup >/dev/null && dump "networksetup_all" sh -c 'networksetup -listallnetworkservices; for s in $(networksetup -listallnetworkservices | tail -n +2); do echo "=== $s ==="; networksetup -getinfo "$s"; done'
            command -v scutil        >/dev/null && dump "scutil_dns" scutil --dns
          fi

          [ -d /etc/netplan ] && { dump "netplan_render" sh -c 'ls -al /etc/netplan; echo; (cat /etc/netplan/*.yaml 2>/dev/null || true)'; }
          [ -f /etc/network/interfaces ] && cp -a /etc/network/interfaces "${OUT_DIR}/interfaces.legacy" || true

          dump "ping_default_gw" sh -c 'gw=$(ip route | awk "/default/ {print \$3; exit}"); [ -n "$gw" ] && (ping -c 2 -W 1 "$gw" || true) || echo "No default gateway"'
          for H in 1.1.1.1 8.8.8.8 google.com cloudflare-dns.com; do dump "nslookup_${H//./_}" sh -c "command -v nslookup >/dev/null && nslookup $H || echo nslookup-missing"; done
          command -v dig >/dev/null  && dump "dig_resolver_test" sh -c 'dig +short @1.1.1.1 example.com; dig +short example.com' || true
          command -v host >/dev/null && dump "host_example" host example.com || true

          { echo "# NET ECHO INDEX ($(date -Is))"; echo; echo "## Directories"; printf "%s\n" "$ECHO_ROOT" "$OUT_DIR" "$BIN_DIR" "$LOG_DIR"; echo; echo "## Files"; find "$OUT_DIR" -type f | sed -e "s|^| - |"; } > "${ECHO_ROOT}/INDEX.md"
          echoe "[ARCHIVE] build"
          tar -czf "${ARCHIVE}" -C "${ECHO_ROOT}" "out" 2>/dev/null || true
          echoe "[DONE] OUT=${OUT_DIR} ARCHIVE=${ARCHIVE}"
          SH
          chmod +x "${ECHO_ROOT}/gather_net_echo.sh"

      - name: "[ECHO] Run Bash collector"
        shell: bash
        env:
          OUT_DIR: ${{ env.ECHO_ROOT }}/out
          BIN_DIR: ${{ env.ECHO_ROOT }}/bin
          LOG_DIR: ${{ env.ECHO_ROOT }}/logs
        run: |
          set -Eeuo pipefail
          export ARCHIVE="${ECHO_ROOT}/net_echo_$(date +%Y%m%d_%H%M%S).tar.gz"
          "${ECHO_ROOT}/gather_net_echo.sh"

      - name: "[ECHO] Upload artifact (Unix)"
        uses: actions/upload-artifact@v4
        with:
          name: net-echo-${{ matrix.os }}
          path: |
            ${{ env.ECHO_ROOT }}/out/**
            ${{ env.ECHO_ROOT }}/INDEX.md
            ${{ env.ECHO_ROOT }}/*.tar.gz
            ${{ env.ECHO_ROOT }}/gather_net_echo.sh
          if-no-files-found: warn
          retention-days: 14

      # --- 항상 현재 리포에 커밋/푸시 ---
      - name: "[ECHO] Commit into current repo (always)"
        shell: bash
        run: |
          set -Eeuo pipefail
          DEST="site/net-echo/${{ matrix.os }}"
          mkdir -p "$DEST"
          cp -a "${ECHO_ROOT}/out" "$DEST/out"
          cp -a "${ECHO_ROOT}/INDEX.md" "$DEST/INDEX.md"
          cp -a "${ECHO_ROOT}"/*.tar.gz "$DEST/" 2>/dev/null || true
          cp -a "${ECHO_ROOT}/gather_net_echo.sh" "$DEST/"
          git config user.name  "net-echo-bot"
          git config user.email "net-echo@users.noreply.github.com"
          git add "$DEST" || true
          git commit -m "[net-echo] ${{ matrix.os }} snapshot: $(date -u +%Y-%m-%dT%H:%M:%SZ) [echo]" || echo "[ECHO] Nothing to commit"
          git push || true

      # --- 외부 리포 업로드: 토큰 없으면 보고서 생성 후 성공 처리 ---
      - name: "[ECHO] External upload to davidkims/springboot (no-fail)"
        shell: bash
        env:
          GH_TOKEN: ${{ env.GH_ADMIN_TOKEN }}
        run: |
          set -Eeuo pipefail
          STATUS_DIR="site/net-echo/status"
          mkdir -p "${STATUS_DIR}"
          if [ -z "${GH_TOKEN}" ]; then
            echo '[ECHO] GH_ADMIN_TOKEN not set; skip external upload.'
            cat > "${STATUS_DIR}/status.json" <<JSON
            {"external_upload":"skipped","reason":"missing GH_ADMIN_TOKEN","target_repo":"${TARGET_REPO_OWNER}/${TARGET_REPO_NAME}","time":"$(date -u +%FT%TZ)","os":"${{ matrix.os }}"}
            JSON
            cat > "${STATUS_DIR}/STATUS.md" <<MD
            # External Upload Status

            - **Result**: Skipped  
            - **Reason**: \`GH_ADMIN_TOKEN\` secret missing  
            - **Target**: ${TARGET_REPO_OWNER}/${TARGET_REPO_NAME}  
            - **OS**: ${{ matrix.os }}  
            - **Time (UTC)**: $(date -u +%F\ %T)  

            > Secrets → **GH_ADMIN_TOKEN**(repo 권한 PAT)을 추가하면 자동 업로드/PR이 생성됩니다.
            MD
            git add "${STATUS_DIR}/status.json" "${STATUS_DIR}/STATUS.md" || true
            git commit -m "[net-echo][status] external upload skipped (no token) ${{
              matrix.os }} $(date -u +%Y-%m-%dT%H:%M:%SZ)" || true
            git push || true

            # (옵션) 리마인드 이슈 생성 시도 — 실패해도 무시
            if command -v gh >/dev/null 2>&1; then
              gh issue create --title "Add GH_ADMIN_TOKEN secret for external upload" \
                --body "External upload to **${TARGET_REPO_OWNER}/${TARGET_REPO_NAME}** skipped.\n\nPlease add \`GH_ADMIN_TOKEN\` (repo-scoped PAT) in **Settings → Secrets and variables → Actions**.\n\nOS: **${{ matrix.os }}**, Run: **#${{ github.run_number }}**" \
                --label "automation,net-echo" || true
            fi
            exit 0
          fi

          # 토큰이 있으면 외부 리포로 브랜치 생성 + PR 시도
          rm -rf ext_repo
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${TARGET_REPO_OWNER}/${TARGET_REPO_NAME}.git" ext_repo
          cd ext_repo
          BR="net-echo-sync/${{ github.run_id }}-${{ matrix.os }}"
          git checkout -b "$BR"
          DEST="uploads/net-echo/${{ github.repository_owner }}/$(echo '${{ github.repository }}' | tr '/' '_')/${{ github.run_number }}/${{ matrix.os }}"
          mkdir -p "$DEST"
          cp -a "../${ECHO_ROOT}/out" "$DEST/out"
          cp -a "../${ECHO_ROOT}/INDEX.md" "$DEST/INDEX.md"
          cp -a "../${ECHO_ROOT}"/*.tar.gz "$DEST/" 2>/dev/null || true
          cp -a "../${ECHO_ROOT}/gather_net_echo.sh" "$DEST/"
          git config user.name  "net-echo-bot"
          git config user.email "net-echo@users.noreply.github.com"
          git add "$DEST"
          git commit -m "[net-echo] import from ${{ github.repository }} #${{ github.run_number }} (${{ matrix.os }})"
          git push -u origin "$BR"
          if command -v gh >/dev/null 2>&1; then
            gh pr create \
              --title "[net-echo] ${{ github.repository }} #${{ github.run_number }} (${{ matrix.os }})" \
              --body  "Auto-imported net echo artifacts.\n- Source: ${{ github.repository }}\n- Run: #${{ github.run_number }}\n- OS: ${{ matrix.os }}\n- Time: $(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              --base "$(git remote show origin | sed -n 's/.*HEAD branch: //p')" \
              --head "$BR" || true
          fi

  # ==================== Windows ====================
  net-echo-windows:
    name: "Windows — Echo (${{ matrix.os }})"
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2025, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - name: "[ECHO] Checkout"
        uses: actions/checkout@v4

      - name: "[ECHO] Runner migration notice (Windows)"
        shell: pwsh
        run: |
          Write-Host "[ECHO] ⚠ windows-latest → Windows Server 2025 마이그레이션(2025-09-02~). 호환 이슈 시 windows-2022로 핀 권장."

      - name: "[ECHO] Prepare dirs"
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "${{ env.ECHO_ROOT }}\out","${{ env.ECHO_ROOT }}\logs" | Out-Null

      - name: "[ECHO] Write PowerShell collector"
        shell: pwsh
        run: |
          $p = "${{ env.ECHO_ROOT }}\Gather-NetEcho.ps1"
          @'
          $ErrorActionPreference = "Stop"
          $ECHO_ROOT = "${{ env.ECHO_ROOT }}"
          $OUT_DIR   = Join-Path $ECHO_ROOT "out"
          $LOG_DIR   = Join-Path $ECHO_ROOT "logs"
          New-Item -ItemType Directory -Force -Path $OUT_DIR,$LOG_DIR | Out-Null
          function Echoe([string]$m){ Write-Host ("[ECHO][" + (Get-Date).ToString("HH:mm:ss") + "] " + $m) }
          function Dump($name, $script){
            $out = Join-Path $OUT_DIR ($name + ".txt")
            Echoe "[RUN] $name -> $out"
            try { & $script | Out-File -FilePath $out -Encoding UTF8 -Width 300 }
            catch { Echoe "[WARN] $name failed: $($_.Exception.Message)" }
          }
          Dump "systeminfo"   { systeminfo }
          Dump "ipconfig_all" { ipconfig /all }
          Dump "route_print"  { route print }
          Dump "arp_a"        { arp -a }
          Dump "netstat_anb"  { netstat -anb }
          Dump "dns_cache"    { ipconfig /displaydns }
          Dump "Get-NetIPConfiguration" { Get-NetIPConfiguration | Format-List * }
          Dump "Get-NetAdapter"         { Get-NetAdapter | Format-List * }
          Dump "Get-NetRoute"           { Get-NetRoute | Sort-Object -Property DestinationPrefix | Format-Table -AutoSize }
          Dump "Get-DnsClientServerAddress" { Get-DnsClientServerAddress | Format-List * }
          Dump "Get-NetFirewallProfile" { Get-NetFirewallProfile | Format-List * }
          Dump "Get-NetFirewallRule"    { Get-NetFirewallRule | Select-Object -First 200 | Format-List * }
          "# NET ECHO INDEX " + (Get-Date).ToString("s") | Out-File (Join-Path $ECHO_ROOT "INDEX.txt")
          Get-ChildItem -Recurse -File $OUT_DIR | ForEach-Object { " - " + $_.FullName } | Out-File -Append (Join-Path $ECHO_ROOT "INDEX.txt")
          $archive = Join-Path $ECHO_ROOT ("net_echo_" + (Get-Date -Format "yyyyMMdd_HHmmss") + ".zip")
          Compress-Archive -Path $OUT_DIR -DestinationPath $archive -Force
          Echoe "[DONE] Out=$OUT_DIR  Archive=$archive"
          '@ | Out-File -FilePath $p -Encoding UTF8

      - name: "[ECHO] Run PowerShell collector"
        shell: pwsh
        run: |
          & "${{ env.ECHO_ROOT }}\Gather-NetEcho.ps1"

      - name: "[ECHO] Upload artifact (Windows)"
        uses: actions/upload-artifact@v4
        with:
          name: net-echo-${{ matrix.os }}
          path: |
            ${{ env.ECHO_ROOT }}\out\**
            ${{ env.ECHO_ROOT }}\INDEX.txt
            ${{ env.ECHO_ROOT }}\*.zip
            ${{ env.ECHO_ROOT }}\Gather-NetEcho.ps1
          if-no-files-found: warn
          retention-days: 14

      - name: "[ECHO] Commit into current repo (always)"
        shell: pwsh
        run: |
          $DEST = "site/net-echo/${{ matrix.os }}"
          New-Item -ItemType Directory -Force -Path $DEST | Out-Null
          Copy-Item -Recurse -Force "${{ env.ECHO_ROOT }}\out" "$DEST\out"
          Copy-Item -Recurse -Force "${{ env.ECHO_ROOT }}\INDEX.txt" "$DEST\INDEX.txt" -ErrorAction SilentlyContinue
          Get-ChildItem "${{ env.ECHO_ROOT }}\*.zip" -ErrorAction SilentlyContinue | ForEach-Object { Copy-Item $_.FullName "$DEST\" }
          Copy-Item -Force "${{ env.ECHO_ROOT }}\Gather-NetEcho.ps1" "$DEST\" -ErrorAction SilentlyContinue
          git config user.name  "net-echo-bot"
          git config user.email "net-echo@users.noreply.github.com"
          git add $DEST
          git commit -m "[net-echo] ${{ matrix.os }} snapshot: $(Get-Date -Format s) [echo]" 2>$null
          git push || $true

      - name: "[ECHO] External upload to davidkims/springboot (no-fail)"
        shell: pwsh
        env:
          GH_TOKEN: ${{ env.GH_ADMIN_TOKEN }}
        run: |
          $STATUS_DIR = "site/net-echo/status"; New-Item -ItemType Directory -Force -Path $STATUS_DIR | Out-Null
          if (-not $env:GH_TOKEN) {
            "[ECHO] GH_ADMIN_TOKEN not set; skip external upload." | Write-Host
            @"
            {"external_upload":"skipped","reason":"missing GH_ADMIN_TOKEN","target_repo":"${{ env.TARGET_REPO_OWNER }}/${{ env.TARGET_REPO_NAME }}","time":"$(Get-Date -Format s)","os":"${{ matrix.os }}"}
            "@ | Out-File -FilePath "$STATUS_DIR\status.json" -Encoding UTF8
            @"
            # External Upload Status

            - **Result**: Skipped  
            - **Reason**: `GH_ADMIN_TOKEN` secret missing  
            - **Target**: ${{ env.TARGET_REPO_OWNER }}/${{ env.TARGET_REPO_NAME }}  
            - **OS**: ${{ matrix.os }}  
            - **Time (local)**: $(Get-Date -Format s)

            > Secrets → **GH_ADMIN_TOKEN**(repo 권한 PAT)을 추가하면 자동 업로드/PR이 생성됩니다.
            "@ | Out-File -FilePath "$STATUS_DIR\STATUS.md" -Encoding UTF8
            git add "$STATUS_DIR\status.json","$STATUS_DIR\STATUS.md"
            git commit -m "[net-echo][status] external upload skipped (no token) ${{ matrix.os }} $(Get-Date -Format s)" 2>$null
            git push || $true

            if (Get-Command gh -ErrorAction SilentlyContinue) {
              gh issue create --title "Add GH_ADMIN_TOKEN secret for external upload" `
                --body "External upload to **${{ env.TARGET_REPO_OWNER }}/${{ env.TARGET_REPO_NAME }}** skipped.`n`nPlease add \`GH_ADMIN_TOKEN\` (repo-scoped PAT) in **Settings → Secrets and variables → Actions**.`n`nOS: **${{ matrix.os }}**, Run: **#${{ github.run_number }}**" `
                --label "automation,net-echo" 2>$null
            }
            exit 0
          }

          if (Test-Path ext_repo) { Remove-Item -Recurse -Force ext_repo }
          git clone "https://x-access-token:${env:GH_TOKEN}@github.com/${{ env.TARGET_REPO_OWNER }}/${{ env.TARGET_REPO_NAME }}.git" ext_repo
          Set-Location ext_repo
          $BR = "net-echo-sync/${{ github.run_id }}-${{ matrix.os }}"
          git checkout -b $BR
          $dest = "uploads/net-echo/${{ github.repository_owner }}/$('${{ github.repository }}' -replace '/', '_')/${{ github.run_number }}/${{ matrix.os }}"
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          Copy-Item -Recurse -Force "..\${{ env.ECHO_ROOT }}\out" "$dest\out"
          Copy-Item -Recurse -Force "..\${{ env.ECHO_ROOT }}\INDEX.txt" "$dest\INDEX.txt" -ErrorAction SilentlyContinue
          Get-ChildItem "..\${{ env.ECHO_ROOT }}\*.zip" -ErrorAction SilentlyContinue | ForEach-Object { Copy-Item $_.FullName "$dest\" }
          Copy-Item -Force "..\${{ env.ECHO_ROOT }}\Gather-NetEcho.ps1" "$dest\" -ErrorAction SilentlyContinue
          git config user.name  "net-echo-bot"
          git config user.email "net-echo@users.noreply.github.com"
          git add $dest
          git commit -m "[net-echo] import from ${{ github.repository }} #${{ github.run_number }} (${{ matrix.os }})"
          git push -u origin $BR
          if (Get-Command gh -ErrorAction SilentlyContinue) {
            gh pr create `
              --title "[net-echo] ${{ github.repository }} #${{ github.run_number }} (${{ matrix.os }})" `
              --body  "Auto-imported net echo artifacts.`n- Source: ${{ github.repository }}`n- Run: #${{ github.run_number }}`n- OS: ${{ matrix.os }}`n- Time: $(Get-Date -Format s)" `
              --base "$(git remote show origin | Select-String 'HEAD branch:' | ForEach-Object { ($_ -split ':')[1].Trim() })" `
              --head $BR  2>$null
          }
